language: python

install:
  - pip install -r requirements.txt
  - ./install_external.sh
  - python setup.py develop

script:
  - flake8 .
  - coverage run -m unittest discover
  - coverage report
  
after_success:
  - coveralls
